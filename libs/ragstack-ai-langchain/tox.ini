[tox]
min_version = 4.0
isolated_build = true
env_list = py311

[testenv]
skip_install = true
allowlist_externals = poetry
commands_pre =
    poetry install
    poetry build
commands =
    poetry run pytest tests/ --import-mode importlib
